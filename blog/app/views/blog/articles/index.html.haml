%h3#all-articles All Articles

- @articles.each do |article|
  .article
    .article-date DD/MM/YYYY
    %h3= article.title
    %p= truncate(article.body, length: 800, separator: ' '){link_to 'Continue Reading', article_path(article)}
