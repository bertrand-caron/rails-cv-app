%h3#all-articles All Articles

- @articles.each do |article|
  .article
    .article-date= article.created_at.strftime("%d %B %Y")
    %h3= article.title
    %p= markdown(truncate(article.body, length: 800, separator: ' '){link_to 'Continue Reading', article_path(article)}, downgrade: 3)
